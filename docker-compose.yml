version: '3'
services:
  web:
    image: "nginx:1.19"
    volumes:
      - ./web/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "8080:8080"
  app:
    build: ./app
    ports:
      - "3000:3000"
  redis:
    image: "redis:6.2"


